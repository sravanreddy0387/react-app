
const initialState = {
    header: 'Welcome top T-Mobile',
    subHeader: 'We are exited to meet you',
    content: {
        contentText: 'Content text'
    }
}

export default (state = initialState, action) => {
    switch (action.type) {

        default:
            return state
    }
}